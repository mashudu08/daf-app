@page
@model ST10115884_MashuduLuvhengo_POE_TASK1.Pages.Donations.MonetaryModel

@{
    ViewData["Title"] = "Monetary";
}
<div class="monetary-section">
    <h2>Monetary</h2>
    <div class="row">
        <div class="col-md-4">
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Monetary.amount" class="control-label"></label>
                    <input asp-for="Monetary.amount" class="form-control" />
                    <span asp-validation-for="Monetary.amount" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Monetary.donationDate" class="control-label"></label>
                    <input asp-for="Monetary.donationDate" class="form-control" />
                    <span asp-validation-for="Monetary.donationDate" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Monetary.donor" class="control-label"></label>
                    <input asp-for="Monetary.donor" class="form-control" />
                    <span asp-validation-for="Monetary.donor" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Donate" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
</div>
<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
